import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  FloatingAddButton: {
    position: 'fixed',
    bottom: 32,
    right: 32,
  },
}));

export default useStyles;
